import styled from "styled-components";

export const CardSubtitleWrapper = styled.h3`
  font-size: 15px;
  margin-top: 5px;
  color: var(--dark-gray);
  font-weight: normal;
  position: relative;
  z-index: 2;
`;
